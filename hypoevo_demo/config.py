"""
HypoEvo 系统配置文件
"""

# ============================================================
# LLM API 配置 — 支持任意 OpenAI 兼容接口
# ============================================================
LLM_CONFIG = {
    "api_key": "your-api-key-here",          # 替换为你的 API Key
    "base_url": "https://api.openai.com/v1",  # 或其他兼容端点
    "model": "gpt-4o",                        # 推荐 gpt-4o / deepseek-chat / glm-4
    "temperature": 0.7,
    "max_tokens": 2048,
}

# ============================================================
# Demo模式：不调用真实API，使用预设模拟数据
# 设为 True 可无需 API Key 运行
# ============================================================
DEMO_MODE = True

# ============================================================
# 辩论锦标赛配置
# ============================================================
TOURNAMENT_CONFIG = {
    "max_debate_rounds": 3,          # 最大辩论轮次
    "hypothesis_count": 3,           # 生成的候选假设数量
    "convergence_threshold": 0.85,   # 辩论收敛阈值（观点一致性）
}

# ============================================================
# 进化记忆配置
# ============================================================
MEMORY_CONFIG = {
    "embedding_dim": 128,            # 记忆向量维度（demo用随机向量）
    "similarity_threshold": 0.6,     # 记忆检索相似度阈值
    "max_entries": 100,               # 最大记忆条目
}
